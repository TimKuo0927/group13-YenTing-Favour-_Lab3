@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model group_13_YenTing_Favour__Lab_3.Models.Episode

@{
    ViewData["Title"] = "Edit Episode";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow-lg border-0 rounded-4">
                <div class="card-body p-4">
                    <h2 class="text-center mb-4 fw-bold text-primary">Edit Episode</h2>

                    @if (ViewBag.VideoUrl != null)
                    {
                        <div class="mb-3 text-center">
                            <video width="100%" height="auto" controls class="rounded-3">
                                <source src="@ViewBag.VideoUrl" type="video/mp4" />
                                Your browser does not support the video tag.
                            </video>
                        </div>
                    }

                    <form asp-action="EditEpisode" asp-controller="Episode" method="post" enctype="multipart/form-data">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="EpisodeId" />
                        <input type="hidden" asp-for="PodcastId" />

                        <div class="mb-3">
                            <label asp-for="Title" class="form-label"></label>
                            <input asp-for="Title" class="form-control" placeholder="Episode Title" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label for="VideoFile" class="form-label">Replace Video File</label>
                            <input type="file" name="VideoFile" class="form-control" accept="video/*" />
                            <small class="form-text text-muted">Leave empty if you don’t want to change the video.</small>
                        </div>

                        <div class="d-grid mt-4">
                            <button type="submit" class="btn btn-primary btn-lg rounded-3">Update Episode</button>
                        </div>

                        

                    </form>
                    <form asp-action="DeleteEpisode" asp-controller="Episode" method="post"
                          onsubmit="return confirmDelete();" class="mt-3">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="EpisodeId" value="@Model.EpisodeId" />
                        <button type="submit" class="btn btn-danger w-100 rounded-3">Delete Episode</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function confirmDelete() {
            return confirm("Are you sure you want to delete this episode? This action cannot be undone.");
        }
    </script>
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
